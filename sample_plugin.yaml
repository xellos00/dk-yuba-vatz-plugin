---
plugins_info:
  plugin_name: "vatz-plugin-near-monitoring"
  plugin_port: 9091  
  default_verify_interval: 60
  default_execute_interval: 30
  executable_apis:
    - method_name: "is_service_up"
      api_interval: 45
